import React from 'react';

import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <p className="all-locations">All locations</p>
      <h2 className="acme-locations">Acme locations</h2>
    </header>
  );
};

export default Header;
